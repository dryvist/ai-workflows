name: Dogfood Local Review
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
permissions:
  contents: read
  pull-requests: write
jobs:
  local-review:
    uses: ./.github/workflows/local-review.yml
    secrets: inherit
