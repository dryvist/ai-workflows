name: Dogfood Scope Shadow Classifier
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
  pull-requests: read
jobs:
  scope-shadow:
    uses: ./.github/workflows/scope-shadow.yml
    secrets:
      TYPESAFE_API_KEY: ${{ secrets.TYPESAFE_API_KEY }}
